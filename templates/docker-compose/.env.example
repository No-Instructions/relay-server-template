# Example environment configuration for minio-tailscale.yaml
# Copy this to .env and customize the values

# Tailscale Configuration
TS_AUTHKEY=your-tailscale-auth-key-here

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme123

# Relay Server Configuration  
RELAY_SERVER_AUTH=your-auth-token-here

# Tailscale URLs - Both should use the same hostname from your tailnet
# Example: if your tailscale machine is "relay-server" in tailnet "mytailnet":
RELAY_SERVER_URL_PREFIX=http://relay-server.mytailnet.ts.net:8080
AWS_ENDPOINT_URL_S3=http://relay-server.mytailnet.ts.net:9000

# Optional overrides
# RELAY_SERVER_STORAGE=s3://relaymd